export function printSectionHeader(line, title) {
    console.log(line);
    console.log(title);
    console.log(line);
    console.log('');
}
export function printKeyValueEntries(entries, options = {}) {
    const indent = options.indent ?? '  ';
    const keys = Object.keys(entries);
    if (keys.length === 0) {
        if (options.emptyMessage) {
            console.log(`${indent}${options.emptyMessage}`);
        }
        return;
    }
    for (const [key, value] of Object.entries(entries)) {
        if (options.multilineJson) {
            const formatted = formatMultilineJson(value, `${indent}  `);
            console.log(`${indent}${key}: ${formatted}`);
        }
        else {
            console.log(`${indent}${key}: ${JSON.stringify(value)}`);
        }
    }
}
function formatMultilineJson(value, indent) {
    return JSON.stringify(value, null, 2).split('\n').join(`\n${indent}`);
}
//# sourceMappingURL=view-utils.js.map