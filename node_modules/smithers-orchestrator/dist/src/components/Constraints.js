import { jsx as _jsx } from "react/jsx-runtime";
/**
 * Constraints component - defines constraints for Claude's responses.
 *
 * Constraints are added to the prompt to guide Claude's behavior.
 *
 * @example
 * ```tsx
 * <Claude>
 *   <Constraints>
 *     - Keep responses concise
 *     - Focus on security
 *     - Cite sources
 *   </Constraints>
 *   Analyze this code
 * </Claude>
 * ```
 */
export function Constraints(props) {
    return (_jsx("constraints", { children: props.children }));
}
//# sourceMappingURL=Constraints.js.map