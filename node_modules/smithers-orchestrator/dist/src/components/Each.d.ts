import type { ReactNode } from 'react';
export interface EachProps<T> {
    items: T[];
    children: (item: T, index: number) => ReactNode;
}
export declare function Each<T>({ items, children }: EachProps<T>): ReactNode;
//# sourceMappingURL=Each.d.ts.map