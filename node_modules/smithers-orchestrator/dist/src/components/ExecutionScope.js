import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useContext } from 'react';
import { useEffectOnValueChange } from '../reconciler/hooks.js';
const ExecutionScopeContext = createContext({ enabled: true });
export function ExecutionScopeProvider(props) {
    return (_jsx(ExecutionScopeContext.Provider, { value: { enabled: props.enabled }, children: props.children }));
}
export function useExecutionScope() {
    return useContext(ExecutionScopeContext);
}
export function useExecutionEffect(enabled, effect, deps = []) {
    useEffectOnValueChange(enabled, () => {
        if (!enabled)
            return;
        return effect();
    }, deps);
}
//# sourceMappingURL=ExecutionScope.js.map