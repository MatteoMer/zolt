import { type ReactNode } from 'react';
export interface CIFailure {
    failed: boolean;
    runId?: string;
    workflowName?: string;
    failedJobs?: string[];
    logs?: string;
}
export interface OnCIFailureProps {
    children: ReactNode;
    /**
     * CI provider (currently only github-actions supported)
     */
    provider: 'github-actions';
    /**
     * Polling interval in milliseconds (default: 30000ms / 30s)
     */
    pollInterval?: number;
    /**
     * Callback when a CI failure is detected
     */
    onFailure?: (failure: CIFailure) => void;
}
export declare function OnCIFailure(props: OnCIFailureProps): ReactNode;
//# sourceMappingURL=OnCIFailure.d.ts.map