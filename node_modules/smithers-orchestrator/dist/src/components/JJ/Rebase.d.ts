import { type ReactNode } from 'react';
export interface RebaseProps {
    destination?: string;
    source?: string;
    onConflict?: (conflicts: string[]) => void;
    children?: ReactNode;
}
/**
 * JJ Rebase component - performs JJ rebase with conflict handling.
 *
 * React pattern: Uses useRef + forceUpdate for fire-and-forget VCS ops.
 * Registers with Ralph for task tracking.
 */
export declare function Rebase(props: RebaseProps): ReactNode;
//# sourceMappingURL=Rebase.d.ts.map