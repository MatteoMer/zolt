import { type ReactNode } from 'react';
export interface WorktreeProps {
    /**
     * Stable identifier for resumability. If not provided, uses branch name.
     * Must be deterministic across restarts - no random IDs.
     */
    id?: string;
    branch: string;
    base?: string;
    path?: string;
    cleanup?: boolean;
    children: ReactNode;
    onReady?: (worktreePath: string) => void;
    onError?: (error: Error) => void;
}
export declare function Worktree(props: WorktreeProps): ReactNode;
//# sourceMappingURL=Worktree.d.ts.map