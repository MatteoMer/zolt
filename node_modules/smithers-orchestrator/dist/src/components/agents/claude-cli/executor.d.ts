import type { CLIExecutionOptions, AgentResult } from '../types.js';
/** Default timeout for Claude CLI execution (5 minutes) */
export declare const DEFAULT_CLI_TIMEOUT_MS = 300000;
/** Default number of schema validation retries */
export declare const DEFAULT_SCHEMA_RETRIES = 2;
/**
 * Execute a single Claude CLI invocation (internal helper)
 */
export declare function executeClaudeCLIOnce(options: CLIExecutionOptions, startTime: number, useApiKey?: boolean): Promise<AgentResult & {
    sessionId?: string;
    shouldRetryWithApiKey?: boolean;
}>;
/**
 * Execute Claude CLI command and return structured result.
 * If a schema is provided, validates the output and retries with --continue on failure.
 */
export declare function executeClaudeCLI(options: CLIExecutionOptions): Promise<AgentResult>;
/**
 * Execute Claude CLI using Bun.$ shell syntax
 * Simpler alternative for basic usage
 */
export declare function executeClaudeShell(prompt: string, options?: Partial<CLIExecutionOptions>): Promise<AgentResult>;
//# sourceMappingURL=executor.d.ts.map