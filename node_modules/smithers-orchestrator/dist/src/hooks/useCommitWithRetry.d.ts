import type { BuildState } from '../db/types.js';
export interface CommitRetryContext {
    state: BuildState;
    error: Error;
}
export interface CommitRetryOptions {
    waitMs?: number;
    staleMs?: number;
    onFixRequested?: (context: CommitRetryContext) => void | Promise<void>;
}
export declare function useCommitWithRetry(options?: CommitRetryOptions): <T>(commitOperation: () => Promise<T>) => Promise<T>;
//# sourceMappingURL=useCommitWithRetry.d.ts.map