import type { ParsedEvent } from './output-parser.js';
export interface FormatterStats {
    phasesCompleted: number;
    agentsExecuted: number;
    toolCalls: number;
    errors: number;
    startTime: Date;
}
export declare class StreamFormatter {
    private stats;
    private lastEventType;
    constructor();
    formatHeader(file: string): string;
    formatEvent(event: ParsedEvent, logPath?: string, summary?: string): string;
    private formatPhase;
    private formatAgent;
    private formatTool;
    private formatRalph;
    private formatError;
    private formatLog;
    formatSummary(duration: number, logDir: string): string;
    private formatTime;
    private formatDuration;
    getStats(): FormatterStats;
}
//# sourceMappingURL=stream-formatter.d.ts.map