import type { RateLimitMonitorConfig, RateLimitStatus, UsageStats, Provider } from './types.js';
export declare class RateLimitMonitor {
    private providers;
    private store;
    private config;
    constructor(config: RateLimitMonitorConfig);
    getStatus(provider: Provider, model: string): Promise<RateLimitStatus>;
    getUsage(executionId: string): Promise<UsageStats>;
    getRemainingCapacity(provider: Provider, model: string): Promise<{
        requests: number;
        inputTokens: number;
        outputTokens: number;
        overall: number;
    }>;
    updateFromHeaders(provider: Provider, model: string, headers: Headers): void;
}
export declare function createRateLimitMonitor(config: RateLimitMonitorConfig): RateLimitMonitor;
//# sourceMappingURL=monitor.d.ts.map