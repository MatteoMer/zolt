import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
export function XMLViewer({ xml, maxLines = 100 }) {
    const lines = xml.split('\n').slice(0, maxLines);
    return (_jsxs("scrollbox", { focused: true, style: { flexGrow: 1 }, children: [lines.map((line, index) => (_jsx("text", { content: line, style: { fg: getLineColor(line) } }, index))), xml.split('\n').length > maxLines && (_jsx("text", { content: `... ${xml.split('\n').length - maxLines} more lines`, style: { fg: '#565f89' } }))] }));
}
function getLineColor(line) {
    const trimmed = line.trim();
    // Comments
    if (trimmed.startsWith('<!--'))
        return '#565f89';
    // Self-closing tags
    if (trimmed.match(/^<[^>]+\/>$/))
        return '#7dcfff';
    // Opening tags
    if (trimmed.match(/^<[^/][^>]*>$/))
        return '#7aa2f7';
    // Closing tags
    if (trimmed.match(/^<\/[^>]+>$/))
        return '#bb9af7';
    // Tag with attributes
    if (trimmed.startsWith('<'))
        return '#7aa2f7';
    // Text content
    return '#c0caf5';
}
//# sourceMappingURL=XMLViewer.js.map