import { type SmithersDB } from '../../db/index.js';
export interface Execution {
    id: string;
    name: string | null;
    status: string;
    file_path: string;
    started_at: string | null;
    completed_at: string | null;
    total_iterations: number;
    total_agents: number;
    total_tool_calls: number;
}
export interface UseSmithersConnectionResult {
    db: SmithersDB | null;
    isConnected: boolean;
    error: string | null;
    currentExecution: Execution | null;
    executions: Execution[];
}
export declare function useSmithersConnection(dbPath: string): UseSmithersConnectionResult;
//# sourceMappingURL=useSmithersConnection.d.ts.map