import { z } from 'zod';
/**
 * Generate system prompt additions for structured output
 */
export declare function generateStructuredOutputPrompt(schema: z.ZodType): string;
/**
 * Generate a retry prompt when validation fails
 */
export declare function generateRetryPrompt(originalOutput: string, validationError: string): string;
//# sourceMappingURL=prompt-generator.d.ts.map