import { z } from 'zod';
import type { ParseResult } from './types.js';
/**
 * Extract JSON from text that may contain markdown code blocks or other content
 */
export declare function extractJson(text: string): string | null;
/**
 * Parse and validate output against a Zod schema
 */
export declare function parseStructuredOutput<T extends z.ZodType>(output: string, schema: T): ParseResult<z.infer<T>>;
//# sourceMappingURL=validator.d.ts.map